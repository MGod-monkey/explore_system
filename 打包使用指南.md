# 无人机自主搜索系统打包指南

## 概述

本指南将帮你将 `start.py` 主程序打包成可执行文件，方便在其他Ubuntu 20.04系统上部署和运行。

## 方法选择

我为你提供了两种打包方法：

### 方法一：简化脚本（推荐）
使用 `simple_build.sh` - 一键打包，适合快速部署

### 方法二：Python脚本
使用 `build_executable.py` - 更详细的控制，适合自定义需求

## 方法一：使用简化脚本（推荐）

### 1. 准备环境

确保你的系统已安装必要组件：

```bash
# 安装PyInstaller
pip3 install pyinstaller

# 安装其他依赖
pip3 install numpy opencv-python psutil

# 确保ROS环境正常
source /opt/ros/noetic/setup.bash
```

### 2. 运行打包脚本

```bash
cd ~/explore_system
./simple_build.sh
```

### 3. 打包过程

脚本会自动：
- ✅ 检查和安装依赖
- ✅ 复制必要文件到构建目录
- ✅ 使用PyInstaller打包
- ✅ 创建发布包
- ✅ 生成启动脚本和说明文档

### 4. 打包结果

成功后会生成 `drone_search_system_release/` 目录，包含：

```
drone_search_system_release/
├── drone_search_system          # 主程序可执行文件
├── run_drone_system.sh         # 启动脚本
├── my_config.rviz              # RViz配置
├── topics_config.json          # 话题配置
├── logo.png                    # 程序图标
├── resource/                   # 资源文件目录
├── ball_screenshots/           # 截图目录
└── README.md                   # 使用说明
```

## 方法二：使用Python脚本

### 1. 运行打包脚本

```bash
cd ~/explore_system
python3 build_executable.py
```

### 2. 按提示操作

脚本会引导你完成整个打包过程，包括依赖检查、文件准备、构建等步骤。

## 使用打包后的程序

### 1. 部署到目标系统

将整个 `drone_search_system_release/` 目录复制到目标Ubuntu 20.04系统。

### 2. 运行程序

```bash
cd drone_search_system_release/
./run_drone_system.sh
```

### 3. 首次运行

程序会：
- 自动检查和启动roscore
- 设置串口权限（需要输入密码）
- 启动主界面

## 注意事项

### 系统要求
- **操作系统**: Ubuntu 20.04
- **ROS版本**: ROS Noetic
- **Python版本**: Python 3.8+

### 依赖要求
目标系统需要安装：
```bash
# ROS Noetic完整安装
sudo apt install ros-noetic-desktop-full

# Python依赖（通常已包含在可执行文件中）
pip3 install numpy opencv-python psutil
```

### 常见问题

#### 1. 权限问题
```bash
chmod +x drone_search_system run_drone_system.sh
```

#### 2. ROS环境问题
```bash
# 检查ROS环境
echo $ROS_PACKAGE_PATH

# 手动设置ROS环境
source /opt/ros/noetic/setup.bash
```

#### 3. 缺少依赖
```bash
# 安装缺少的ROS包
sudo apt install ros-noetic-[package-name]

# 安装Python依赖
pip3 install [package-name]
```

#### 4. 串口权限
```bash
# 手动设置串口权限
sudo chmod 777 /dev/ttyACM0
```

## 高级配置

### 自定义打包选项

如果需要自定义打包选项，可以修改 `simple_build.sh` 中的PyInstaller参数：

```bash
pyinstaller --onefile \
    --name="your_custom_name" \
    --add-data="your_file:." \
    --hidden-import="your_module" \
    start.py
```

### 添加额外文件

在打包脚本中添加你需要的文件：

```bash
# 在simple_build.sh中添加
cp your_additional_file.txt "$BUILD_DIR/"
```

## 测试验证

### 1. 本地测试
在打包完成后，先在本地测试：

```bash
cd drone_search_system_release/
./run_drone_system.sh
```

### 2. 目标系统测试
在目标系统上测试所有功能：
- ✅ 程序启动
- ✅ RViz显示
- ✅ 话题订阅
- ✅ 截图功能
- ✅ 控制功能

## 故障排除

### 打包失败
1. 检查所有依赖文件是否存在
2. 确认ROS环境正确设置
3. 查看PyInstaller错误信息

### 运行失败
1. 检查目标系统ROS环境
2. 验证文件权限
3. 查看程序输出的错误信息

### 功能异常
1. 确认配置文件正确复制
2. 检查资源文件路径
3. 验证ROS话题连接

## 优化建议

### 减小文件大小
- 使用 `--exclude-module` 排除不需要的模块
- 压缩资源文件
- 移除调试信息

### 提高启动速度
- 使用 `--onedir` 而不是 `--onefile`
- 预编译Python模块
- 优化导入顺序

打包完成后，你就可以将程序部署到任何Ubuntu 20.04系统上运行了！
